import { CampaignTypeReducer } from './campaign-type.reducer';

describe('CampaignTypeReducer', () => {
  it('should create an instance', () => {
    expect(new CampaignTypeReducer()).toBeTruthy();
  });
});
