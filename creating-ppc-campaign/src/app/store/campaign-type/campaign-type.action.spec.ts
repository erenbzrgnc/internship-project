import { CampaignTypeAction } from './campaign-type.action';

describe('CampaignTypeAction', () => {
  it('should create an instance', () => {
    expect(new CampaignTypeAction()).toBeTruthy();
  });
});
