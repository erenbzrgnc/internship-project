import { CampaignTypeSelector } from './campaign-type.selector';

describe('CampaignTypeSelector', () => {
  it('should create an instance', () => {
    expect(new CampaignTypeSelector()).toBeTruthy();
  });
});
