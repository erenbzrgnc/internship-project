import { CampaignTypeEffect } from './campaign-type.effect';

describe('CampaignTypeEffect', () => {
  it('should create an instance', () => {
    expect(new CampaignTypeEffect()).toBeTruthy();
  });
});
